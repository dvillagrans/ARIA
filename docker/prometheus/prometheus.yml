global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "aria-fastapi"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["fastapi:8000"]
